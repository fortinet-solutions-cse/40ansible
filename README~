# A few cli to play with:


#Docekr test/dev 
docker build  -t 40ansible .

#Run
docker run -i -t 40ansible /bin/bash

#To mount a local directory for playbook dev/test :
docker run -v /Users/thomas/src/40ansible/:/40ansible -i -t 40ansible /bin/bash

#Debugging API calls on your fortigate:
diag debug application httpsd 255
diag debug enable
